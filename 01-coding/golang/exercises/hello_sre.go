package main

/*
Hello SRE - Go Edition (Exercise)

OBJECTIVE:
Create a simple Go program that demonstrates basic SRE concepts in Go.

REQUIREMENTS:
1. Display a welcome message for SRE training
2. Show system information:
   - Hostname
   - Operating System
   - Architecture
   - Go version
   - Number of CPUs
   - Current time
3. Format output nicely with borders and sections
4. Handle errors properly (especially for hostname lookup)

HINTS:
- Use os.Hostname() for hostname
- Use runtime.GOOS for operating system
- Use runtime.GOARCH for architecture
- Use runtime.Version() for Go version
- Use runtime.NumCPU() for CPU count
- Use time.Now() for current time
- Use fmt.Println() and fmt.Printf() for output
- Use strings.Repeat() for creating borders
- Handle errors: if err != nil { ... }

TODO: Implement the program below
*/

import (
	// TODO: Import required packages:
	// - fmt
	// - os
	// - runtime
	// - strings
	// - time
)

func main() {
	// TODO: Display welcome header with border (using = characters)
	
	
	// TODO: Get hostname (handle error if it occurs)
	
	
	// TODO: Display system information section
	// Show: hostname, OS, architecture, Go version, CPU count, current time
	
	
	// TODO: Display closing border
	
}
